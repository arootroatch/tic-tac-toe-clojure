(ns tic-tac-toe.gui.play-spec
  (:require [speclj.core :refer :all]
            [tic-tac-toe.gui.play :refer :all]))

(describe "play screen"
  (context "three-board"
    (xit "displays current board"
      ( )))

  (context "create-square"
    (it "displays one square of the board"
      (should=))))

